## 1. 架构目录与基础配置

- [x] 1.1 创建后端、前端、数据、脚本、测试和文档目录结构。
- [x] 1.2 增加 `.env.example`，列出 MySQL、Redis、模型路径、数据路径和服务端口配置。
- [x] 1.3 增加根目录 `.gitignore`，排除训练数据、模型文件、缓存、虚拟环境和前端构建产物。
- [x] 1.4 更新 README，说明后端启动、前端启动、训练脚本、数据库初始化和 API 调用方式。

## 2. Flask 后端骨架

- [x] 2.1 创建 Flask 应用工厂和统一配置加载模块。
- [x] 2.2 创建统一 JSON 响应结构和错误处理逻辑。
- [x] 2.3 创建 API、service、repository、ML 模块分层。
- [x] 2.4 增加 CORS 配置，支持 Vue 本地开发环境访问。
- [x] 2.5 在复杂配置和错误处理逻辑中加入简洁中文注释。

## 3. 数据库与缓存

- [x] 3.1 创建 MySQL 初始化 SQL，包含 `knowledge_points`、`questions`、`prediction_logs` 表。
- [x] 3.2 初始化 15 个高中生物必修2知识模块数据。
- [x] 3.3 创建 MySQL 连接和 repository 基础封装。
- [x] 3.4 创建 Redis 连接和预测缓存封装。
- [x] 3.5 Redis 不可用时实现降级逻辑，并使用中文注释说明降级意图。

## 4. BERT 模型模块

- [x] 4.1 创建训练数据目录约定，包含 `train.txt`、`dev.txt`、`test.txt`、`class.txt`。
- [x] 4.2 创建数据校验脚本，检查文本与标签格式。
- [x] 4.3 创建 BERT 训练脚本骨架，支持保存最佳模型和输出评估报告。
- [x] 4.4 创建在线推理模块骨架，负责加载模型、标签和执行单条预测。
- [x] 4.5 在模型加载和推理关键逻辑中加入简洁中文注释。

## 5. 后端 API

- [x] 5.1 实现 `POST /api/v1/predict/knowledge-point` 预测接口。
- [x] 5.2 实现 `GET /api/v1/knowledge-points` 知识点列表接口。
- [x] 5.3 实现 `GET /api/v1/predictions/recent` 最近预测记录接口。
- [x] 5.4 实现 `GET /api/v1/stats/overview` 统计概览接口。
- [x] 5.5 实现 `GET /api/v1/health` 健康检查接口。
- [x] 5.6 为 API 增加基础测试或调用样例。

## 6. Vue 前端骨架

- [x] 6.1 使用 Vue 3 + Vite 创建前端项目骨架。
- [x] 6.2 创建 API 调用封装，统一处理后端响应结构。
- [x] 6.3 创建预测工作台页面，包含题目输入、预测按钮和清空按钮。
- [x] 6.4 创建预测结果卡片，展示知识模块、置信度、耗时和缓存命中状态。
- [x] 6.5 创建知识点详情、最近预测记录、知识点列表、服务状态和统计展示区域。
- [x] 6.6 处理 loading、错误提示和预测成功后的记录刷新。

## 7. 联调与验收

- [x] 7.1 使用样例数据完成一次训练或推理流程验证。
- [x] 7.2 完成 Flask API 与 Vue 页面联调。
- [x] 7.3 验证 MySQL 预测记录写入。
- [x] 7.4 验证 Redis 缓存命中和不可用降级。
- [x] 7.5 检查关键代码中的中文注释是否简洁且必要。
- [x] 7.6 运行可用的后端测试、前端构建或接口冒烟测试。
